// GRID settings: the only file to edit at launch. Empty = not set yet (the site shows it honestly).
module.exports = {
  name: 'GRID',
  ticker: '$GRID',
  ca: '',   // token mint once it exists
  x: '',    // project X handle, without @
};
